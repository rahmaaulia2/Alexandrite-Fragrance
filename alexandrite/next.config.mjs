/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['onixfragrance.co.id'], // Ganti 'example.com' dengan domain yang Anda daftarkan
    },
};

export default nextConfig;
